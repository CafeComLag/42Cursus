#ifndef LIBFT_H
# define LIBFT_H

#include <unistd.h>
#include <stddef.h>
#include <stdlib.h>
#include <limits.h>

void	*ft_memset(void *s, int c, size_t n);
void	*ft_calloc(size_t n, size_t size);

#endif
